<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="action_product_limit_duplicate" model="ir.actions.server">
        <field name="name">Mass duplicate</field>
        <field name="model_id" ref="model_lims_parameter_char_product"/>
        <field name="binding_model_id" ref="model_lims_parameter_char_product"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.mass_duplicate()
        </field>
    </record>

</odoo>
